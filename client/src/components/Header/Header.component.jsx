import React from 'react';
// Pages
// Components
import Navbar from '../Navbar/Navbar.component';
// CSS

const Header = () => {
   return (
      <header>
         <Navbar />
      </header>
   );
};

export default Header;